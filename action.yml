name: 'Setup GraalVM environment'
description: 'Setup your runner with GraalVM'
author: 'DeLaGuardo'
branding:
  icon: 'gift'
  color: 'blue'
inputs:
  graalvm:
    description: 'The GraalVM version, defaults to latest'
    default: 'latest'
runs:
  using: 'node12'
  main: 'dist/index.js'
